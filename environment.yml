# To use:
#   $ conda env create -f environment.yml  # `mamba` works too for this command
#   $ conda activate scipy-dev
#
name: gulinalg
channels:
  - conda-forge
dependencies:
  - python==3.11
  - compilers
  - pkg-config
